production:
    rails:
        configuration:
            do_initial_db_schema_load: true
development:
    rails:
        configuration:
            do_initial_db_schema_load: true
staging:
    rails:
        configuration:
            do_initial_db_schema_load: true
