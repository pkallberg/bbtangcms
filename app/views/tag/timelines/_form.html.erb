<%= simple_form_for([:tag,@identity,@tag_timeline]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <!--%= f.input :parent_id %-->
    <!--%= f.input :created_at %-->
    <!--%= f.input :updated_at %-->
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary', :disable_with => 'loading...' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tag_identity_timelines_path(@identity), :class => 'btn' %>
  </div>

<% end %>
